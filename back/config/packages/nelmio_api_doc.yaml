nelmio_api_doc:
    documentation:
        info:
            title: users api
            description: just a simple rest api app
            version: 0.1.0
    areas: # to filter documented areas
        path_patterns:
            - ^/api(?!/doc$) # Accepts routes under /api except /api/doc
